# Copy to .env and fill in values. Required vars must be set for the app to start.
# ORACLE_DID is derived at runtime from MATRIX_ORACLE_ADMIN_USER_ID (do not set it here).

# --- App ---
NODE_ENV=development
PORT=3000
ORACLE_NAME=MyOracle
# Allowed CORS origin (default: *)
CORS_ORIGIN=*

# --- Matrix (required) ---
MATRIX_BASE_URL=https://your-matrix-homeserver.example
MATRIX_RECOVERY_PHRASE=
MATRIX_ORACLE_ADMIN_ACCESS_TOKEN=
MATRIX_ORACLE_ADMIN_USER_ID=@oracle:your-homeserver
MATRIX_ORACLE_ADMIN_PASSWORD=
# Local paths for Matrix client state (defaults shown)
MATRIX_STORE_PATH=./matrix-store-new
MATRIX_CRYPTO_STORE_PATH=./matrix-storage
MATRIX_SECRET_STORAGE_KEYS_PATH=./matrix-secret-storage-keys-new2
MATRIX_VALUE_PIN=
MATRIX_ACCOUNT_ROOM_ID=

# --- Slack (optional) ---
# If SLACK_BOT_OAUTH_TOKEN is not set, Slack integration is disabled.
# When using socket mode (SLACK_USE_SOCKET_MODE=true), SLACK_APP_TOKEN is required.
# SLACK_BOT_OAUTH_TOKEN=
# SLACK_APP_TOKEN=
# SLACK_USE_SOCKET_MODE=true
# SLACK_MAX_RECONNECT_ATTEMPTS=10
# SLACK_RECONNECT_DELAY_MS=1000

# --- LLM / AI ---
# Optional; used by @ixo/common when OpenAI models are needed
# OPENAI_API_KEY=
OPEN_ROUTER_API_KEY=

# --- Blockchain / IXO ---
NETWORK=devnet
RPC_URL=https://devnet.ixo.earth/rpc/
SECP_MNEMONIC=
ORACLE_ENTITY_DID=did:ixo:entity:
# Optional: Blocksync GraphQL (used by chain client when set)
# BLOCKSYNC_URI=
# Optional: subscription service URL (fallback from NETWORK if unset)
# SUBSCRIPTION_URL=
# Optional: MCP URL for subscription oracle
# SUBSCRIPTION_ORACLE_MCP_URL=
# Set to "true" to disable credits/subscription checks (default: false)
DISABLE_CREDITS=false

# --- Memory & MCP ---
MEMORY_MCP_URL=http://localhost:8988/mcp
MEMORY_ENGINE_URL=http://localhost:8988
MEMORY_SERVICE_API_KEY=
FIRECRAWL_MCP_URL=
DOMAIN_INDEXER_URL=
SANDBOX_MCP_URL=

# --- Data & storage ---
SQLITE_DATABASE_PATH=./data/oracle.sqlite
# Optional: when unset, task scheduling, token limiting, and claim processing are disabled
REDIS_URL=redis://localhost:6379
# Set to "true" for TLS to DB (default: false)
DATABASE_USE_SSL=false

# --- Live agent ---
# API key used to validate live-agent / calls requests (optional)
# LIVE_AGENT_AUTH_API_KEY=

# --- UCAN (optional) ---
# Comma-separated DIDs allowed as root issuers for MCP UCAN auth
# UCAN_ROOT_ISSUERS=
# Comma-separated MCP server names that require UCAN authorization
# UCAN_PROTECTED_MCP_SERVERS=
