---
'@ixo/oracle-runtime-workers': minor
---

Bound Workers turns across main/helper/child models, serialize unknown writes, retain uncertain operation receipts, offload large results to user-owned SQLite, and terminate context recovery after one smaller attempt. Propagate cancellation to subagents and settle aborted SSE actions. Expose a prompt-only import for instance contract tests.
