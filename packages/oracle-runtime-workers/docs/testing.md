# Testing

Four layers, from milliseconds to minutes. Run the targeted layer while
iterating and the full matrix once at the end.

## Unit tests (seconds)

- `pnpm test` — runs **inside workerd** through `@cloudflare/vitest-pool-workers`
  (`vitest.config.ts`, test worker in `test/worker.ts`, bindings in
  `test/wrangler.test.jsonc`): the SQLite VFS and saver (20 MiB files,
  eviction survival, legacy-file load), the ingest pipeline, reply chains,
  the room-state codec, the task scheduler over real SQLite, the socket.io
  endpoint over real WebSockets, the shell, and the editor and flows plugin
  suites that must prove the linkedom bridge on the real runtime.
- `pnpm test:core` — plain-Node suites (`vitest.core.config.ts`): plugin
  loader, env composition, registries, middlewares, prompt composer, a full
  tool-calling turn against a fake model, the memory indexer, and the
  bundled-plugin tests that mock the MCP and Composio SDKs.
- `pnpm typecheck`, `pnpm lint`, and `pnpm format:check` at the repo root.

The Matrix gateway has no unit tests here any more: its generic behaviour is
tested in `@ixo/matrix-bot-workers-sdk`; the oracle-specific parts are
covered by the devnet matrix below.

## Harness end-to-end (minutes, local)

From `apps/qiforge-workers-example`, against the ixo testing harness and a
real LLM:

| Script                       | Covers                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `pnpm test:e2e`              | Auth, streaming, tools, memory, transcript, abort, owner-copy round-trip, E2EE chat (the reply threaded on the message, a threaded follow-up continuing its session, a bare message opening a new one), the reset-leak drills (room mirror across a gateway reset, dirty mark after an interrupted turn and object reset, the `delegation_required` prompt and its throttle), device-stable restart, a live scheduled task fired by a DO alarm.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `pnpm test:e2e:vfs`          | The default owner-store path: a real VFS worker, the user's delegation carrying `ixo:filesystem/.oracles`, flush → file in the user's VFS → reload, Matrix untouched, 403 without the capability.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `pnpm test:e2e:mcp`          | A real Streamable-HTTP MCP handshake from workerd with per-user UCAN headers, cross-session memory recall.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `pnpm test:e2e:tier`         | The R2 page tier end to end (miniflare's local R2): chat turns, a forced eviction pass, cold reads through the transcript and a follow-up turn on the evicted session, flush to the VFS from a tiered file, reset + reload, and the R2 prefix cleared on wipe. Unit-level coverage is `src/sqlite/page-tier.test.ts` (eviction, partial rows, snapshots, truncation, orphans, a 24 MB file).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `pnpm test:e2e:durable`      | Durable runs (`docs/plans/durable-runs.md`) with the drill plugin (`src/drill-plugin.ts`, `DRILL_TOOLS=true`) and a recorder counting tool executions: run frames and ids; a client that disconnects while the run finishes; a re-join with a cursor that receives exactly the rest; a user-object reset mid-reply (recovered, continued, finished); a reset mid-write tool (executed once, "outcome unknown" to the model) and mid-read tool (executed again); supersede vs `enqueue`; the recovery cap (interrupted with partial text); a scheduled task run reset mid-tool, recovered and delivered once. `STEP_FILTER` narrows it. With `ACCOUNT_JSON` + `ORACLE_URL` + `ORACLE_DID` (and `RECOVERY_ATTEMPTS` / `RECOVERY_DELAYS_MS` matching the deployment) the same drill runs against a deployed oracle that has `DRILL_TOOLS=true` and `ORACLE_DEBUG_ROUTES=true`; there the resets are real (`ctx.abort()`) and tool executions are counted from the runtime's tool marks instead of the recorder.                                                                                                                                                                                                                                                                                                                                                       |
| `pnpm test:e2e:context`      | Context budgets (`docs/plans/context-budgets.md`) with the drill plugin's `drill_big_result`: `GET /debug/context` resolves the window per model and derives every threshold from it; a tool result above the cap is truncated to head + tail, saved whole, and the model reads the omitted middle back with `read_result`; a multi-megabyte result lands in the R2 tier; on a model whose window is pinned small by `MODEL_CONTEXT_OVERRIDES` (locally the default model at 32k, on devnet `google/gemini-3.1-flash-lite`, or `PRESSURE_MODEL=`; the model must be in the runtime's `MODEL_CATALOG`) old results are pruned under pressure and the history is summarized at the window fraction with no message trigger — asserted through the per-session counters of `GET /debug/context?session=`, never the logs (`wrangler dev` does not forward `console.log`). `CONTEXT_MODELS=a,b,c` picks the models; against a deployment (`ACCOUNT_JSON` + `ORACLE_URL` + `ORACLE_DID`) it drills the default model plus two more with different windows from `GET /models` (name them with `CONTEXT_MODELS` when the deployment lists none).                                                                                                                                                                                                                          |
| `pnpm test:e2e:threads`      | Room threads as sessions ([operations](operations.md#turns-threads-are-sessions)): a bare message is answered inside a thread rooted at it, that thread is a session with the root event id (listed by `GET /sessions`, readable through the transcript route), a threaded reply and a quote-reply to the bot's answer continue it, a new bare message is a new session, an HTTP turn on a room-born session is mirrored into its thread, and a reply inside a Portal session's thread continues the Portal session. Local by default (a fresh E2EE device via `test/lib/matrix-client.ts`); against a deployment with `ACCOUNT_JSON` (the account file's `roomId` and Matrix password) + `ORACLE_URL` + `ORACLE_DID`, optionally `MATRIX_URL` / `BOT_MATRIX_USER_ID`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `pnpm test:e2e:group`        | Matrix group rooms ([operations](operations.md#rooms-group-chats)): a fresh E2EE room with two people and the bot — the bot joins its invite, a message without a mention gets no reply, a mention is answered inside a thread and the stored message carries `[Alice]: ` and the `(USER MENTIONED YOU @AI_AGENT)` rewrite, a thread follow-up without a mention is answered, a quote-reply to the bot's answer from the other member is answered, the other member's bare messages are captured but not answered, the bot pins and recalls a fact, the captured messages are compacted and found by `search_channel_memory`, and no bot message sits in the main timeline; the bot is kicked at the end. Locally the oracle boots with `MATRIX_GROUP_ROOMS=gate`; `GROUP_ROOMS=silent` boots the default policy instead and proves a mention, a bare message and a quote-reply all get no reply and capture nothing. Against a deployment with `ACCOUNT_JSON` + `MEMBER_JSON` (a second account, passive unless `MEMBER_CHATS=1`) + `ORACLE_URL` + `ORACLE_DID`, pass `GROUP_ROOMS=silent` when the gateway runs the default. Unit coverage is `src/matrix/group-chat.test.ts` (store, FTS5 + LIKE, room kinds, display names, every gate outcome, durable active threads, compaction thresholds, JIT cap, facts) and `src/plugins/matrix-group-chats/*.test.ts`. |
| `pnpm test:e2e:transcript`   | Transcript paging (`docs/plans/transcript-paging.md`): `GET /sessions/:id/messages` pages a seeded session by turns, the pages tile the legacy `GET /messages/:id` listing exactly with no split tool call, `after=` returns nothing until a new turn and then that turn whole, bad limits and cursors are 400s, an unknown session is an empty page, `limit` clamps at 100. Local by default; against a deployment with `ACCOUNT_JSON` + `ORACLE_URL` + `ORACLE_DID`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `pnpm test:e2e:legacy-large` | A ≥ 64 MB gzipped Node-era Matrix checkpoint (synthetic, saver schema, uploaded as the bot from a second device) imported on the VFS path: streamed in, flushed to the VFS, legacy copy redacted, workerd's memory growth bounded by the file, the seeded session listed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `test/e2e-migration.ts`      | Node → Workers migration of a user's history including the key backup and a token rotation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `pnpm test:stress`           | N users concurrently, per-user isolation checks.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |

## The devnet feature matrix (minutes, deployed worker)

`apps/qiforge-workers-example/test/devnet-features.ts` drives the deployed
worker through every feature: health, auth, models, delegation, sessions,
streaming and JSON turns, transcript, thread memory, titles, abort,
memory-engine, sandbox, skills, flows, VFS round-trip, firecrawl and
domain-indexer sub-agents, per-room secrets, the owner-store flush plus
reset/reload, E2EE Matrix ingress, scheduled tasks including a dedicated
room, HTTP and Matrix attachments with payload retention, user preferences,
the Node room-state envelope, the socket.io channel (handshake, browser tool
and AG-UI round trips, `create_page_room` with the CRDT edit replayed),
gateway restarts (catch-up, creates during a restart), the reset-safety
drills (gateway and user-object hard resets mid-turn) and the reset-leak
drills (the room mirror under a burst of gateway resets, the dirty mark
after an interrupted turn, the `delegation_required` prompt), the
validation and auth-boundary edge cases, cross-user isolation and the
per-user rate limit.

```bash
cd apps/qiforge-workers-example
ACCOUNT_JSON=<account.json> [ORACLE_URL=https://…] pnpm exec tsx test/devnet-features.ts
# one step or a group:
STEP_FILTER='^gateway restart' ACCOUNT_JSON=… pnpm exec tsx test/devnet-features.ts
```

- Accounts live in the gitignored `test/.devnet-accounts/` (override with
  `SCRATCH_DIR`): the main user in `devnet-account.json` (or `ACCOUNT_JSON`),
  each a devnet test account (`did`, `address`, `edSigningMnemonic`,
  `matrixUserId`, `matrixPassword`, `roomId`) created with the testing
  harness's `devnet-mig-account.mjs`. Two more are read from the same
  directory: `devnet-user-2.json` as the "other user" and `devnet-user-6.json`
  as the user whose delegation lacks the file-storage capability — never
  give that one a delegation carrying `ixo:filesystem`, or a working copy.
- A full run takes 15–20 minutes; split it with `STEP_FILTER` when a shell
  or CI step has a shorter limit. Some steps depend on earlier ones in the
  same run (the edge steps need "edge: create a scratch session"; the
  cross-user step needs the scratch session) — filter groups, not single
  steps, for those.
- Failures print the assertion and, for transport errors, the underlying
  cause. A single "fetch failed" from the client machine is not a runtime
  failure; re-run the step.

## Stress (minutes, deployed worker)

The load scripts are in `test/load/` and read the accounts from
`test/.devnet-accounts/` (`stress-users.mts`; `outbox-probe.mts`; the BYO
lane end-to-end scripts; `delete-stale-devices.py` to retire the bot's stale
devices). `stress-users.mts` phases: 1 = everyone
creates sessions at once, 2 = everyone runs N turns in parallel, 2b = half
the users create sessions while the other half chat, 3 = synchronized
bursts, 4 = reads. `PHASES=2,2b TURNS=4` is the quick run; the numbers to
compare are in [load tests](load-tests.md). Watch the gateway alongside:

```bash
pnpm exec wrangler tail <gateway script> --format json > tail.jsonl
GET /matrix/status   # instanceId unchanged, sendScheduler.failed 0, rateLimited 0
```

## Reproduction objects

`test/hibernation-probe/` holds a minimal Durable Object plus its wrangler
config for checking whether an MCP round trip leaves an object resident:
deploy it with that config, call `/mcp-opt/timeout` vs `/mcp-opt/own`, and
compare `instanceId` after 45 s idle. Delete the deployed copy afterwards.
