/**
 * Example plugins that prove the Workers plugin API end-to-end. Neither is
 * loaded unless a host passes it to `createRuntimeCore({ plugins })`.
 */
export * from './weather';
export * from './skills';
